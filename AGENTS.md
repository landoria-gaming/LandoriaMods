1. Before doing any work, read and follow the entire `AGENTS.global.md` file.
2. Keep each plugin's behavior, single `README.md` used by both the repository and Thunderstore package, manifest, categories, client/server requirements, Contact section, and clear non-flat 256x256 icon synchronized. Keep licenses for public-source plugins. Do not create or retain `README.Thunderstore.md`. Changelogs are optional and may omit versions; only the ModPack changelog is required, and automation must generate its added, updated, and removed dependency entries. Keep `PluginVersion`, manifest `version_number`, `AssemblyFileVersion`, and the first three `AssemblyVersion` components identical.
3. For local runtime tests, use the authorized Valheim server in `C:\Program Files (x86)\Steam\steamapps\common\Valheim dedicated server`. Before every test, copy each rebuilt required mod DLL to both the server `BepInEx\plugins` folder and `BepInEx\config\ModSentry_Required`, then copy the same DLL to the shared client profile in `C:\Users\npuzi\AppData\Roaming\r2modmanPlus-local\Valheim\profiles\local\BepInEx\plugins`. Verify that all copies have the same hash and complete assembly version. Close both Valheim clients before replacing locked DLLs.
4. Local multiplayer tests use two clients, Steam and Xbox, with the same r2modman profile. Launch them with `C:\Users\npuzi\Desktop\Valheim Steam.bat` and `C:\Users\npuzi\Desktop\Valheim Xbox.bat`. Review their separate logs at `logs\steam-player.log` and `logs\xbox-player.log` inside the shared profile, together with the dedicated server `BepInEx\LogOutput.log`. Start Normal with `start_server_normal.bat` or Hammer with `start_server_hammer.bat`, keep the server command session running, and verify the server process and log after every restart.
